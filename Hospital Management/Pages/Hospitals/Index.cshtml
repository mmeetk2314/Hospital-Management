@page
@model Hospital_Management.Pages.Hospitals.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hospital[0].HospitalName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hospital[0].HospitalBranch)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hospital[0].ContactNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hospital[0].OpeningDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hospital[0].Floors)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Hospital) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HospitalName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HospitalBranch)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OpeningDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Floors)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.HospitalID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.HospitalID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.HospitalID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
