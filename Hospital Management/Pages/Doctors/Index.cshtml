@page
@model Hospital_Management.Pages.Doctors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].DoctorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].ContactNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].JoiningDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Salary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Hospital_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Doctor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DoctorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JoiningDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hospital_ID.HospitalID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DoctorID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DoctorID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DoctorID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
